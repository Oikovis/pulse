# Test dependencies for Oikovis Pulse
# Install into a Python 3.14+ virtual environment:
#   python3.14 -m venv .venv
#   source .venv/bin/activate
#   pip install -r requirements-test.txt

pytest==8.3.3
pytest-asyncio==0.24.0
# homeassistant 2026.9.0 requires Python >=3.14.2.
# Declared minimum in hacs.json is 2026.4.0, but the suite tests only against 2026.9.0.
# Floor verification is a known gap.
homeassistant==2026.9.0
